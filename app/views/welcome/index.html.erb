<h1></h1>
<% if user_signed_in? %>
	<ul>
	<b> Bem-vindo </b> <%= current_user.email %>
	<br>
	<%= l Date.today %>
	<br>
	<br>
		<span class="glyphicon glyphicon-off" aria-hidden="true"></span><%= link_to "         Sair", destroy_user_session_path, method: :delete %>
		<br>
		<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span><%= link_to "         Editar usuário", edit_user_registration_path %>
		
	</ul>
<% else %>
		<span class="glyphicon glyphicon-user" aria-hidden="true"></span><%= link_to "     Logar", new_user_session_path %>
<% end %>
<div id="logo"></div> 
